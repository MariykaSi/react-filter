import { StyleSheet } from "aphrodite/no-important";

export default StyleSheet.create({
  userCard: {
    maxWidth: "1200px",
    margin: "0 auto",
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap"
  }
});
